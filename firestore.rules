
rules_version = '2';

service cloud.firestore {
  match /databases/{database}/documents {
    // Default rule: All paths require user to be authenticated
    match /{document=**} {
      allow read, write: if request.auth != null;
    }
  }
}
